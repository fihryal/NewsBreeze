<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NewsBreeze" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">@color/black</item>
    </style>

    <style name="Theme.NewsBreeze" parent="Base.Theme.NewsBreeze" />

    <style name="Theme.NewsBreeze.Search" parent="Base.Theme.NewsBreeze">
        <item name="android:statusBarColor">@color/white_cream</item>
        <item name="android:windowLightStatusBar">@color/black</item>
    </style>
</resources>